{
  "private": true,
  "name": "synapse-app",
  "version": "1.0.0",
  "description": "Synapse teacher app (static front-end + Netlify Functions)",
  "scripts": {
    "build:noop": "echo \"No build step\""
  },
  "dependencies": {
    "@google/generative-ai": "^0.19.0"
  },
  "engines": {
    "node": ">=18"
  }
}
